name: notification_api
description: "A new Flutter package project."
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter_local_notifications: ^13.0.0
  timezone: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0